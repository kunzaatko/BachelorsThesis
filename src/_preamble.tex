\synctex=1
% TODO: When finished, search for "??" to identify missing references <22-11-23> 
% Load the kaobook class
\documentclass[
	fontsize=10pt,
	footnotesize=8pt,
	twoside=true,
	open=any, % uncomment this to force new chapters to start on any page, not only on right (odd) pages
    chapterentrydots=true, % dots in the TOC
	secnumdepth=1, % How deep to number headings. Defaults to 1 (sections)
    fontmethod=modern,
]{kaobook}

\makeatletter
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%% Textclass specific LaTeX commands.
\newenvironment{lyxlist}[1]
	{\begin{list}{}
		{\settowidth{\labelwidth}{#1}
		 \setlength{\leftmargin}{\labelwidth}
		 \addtolength{\leftmargin}{\labelsep}
		 \renewcommand{\makelabel}[1]{##1\hfil}}}
	{\end{list}}
\makeatother

\usepackage{datetime}
\newdate{date}{9}{1}{2024}
\date{\displaydate{date}}
% Choose the language
\usepackage[english]{babel} % Load characters and hyphenation
\usepackage[english=british]{csquotes}	% English quotes

% Load packages for testing
%\usepackage{showframe} % Uncomment to show boxes around the text area, margin, header and footer
%\usepackage{showlabels} % Uncomment to output the content of \label commands to the document where they are used

% Load the bibliography package
\usepackage[addspace=true, linkeverything=true]{kaobiblio}

\addbibresource{bibliography.bib}

% Load mathematical packages for theorems and related environments
\usepackage{kaotheorems}

% Load the package for hyperreferences
\usepackage{kaorefs}
\usepackage{adjustbox}

\graphicspath{{figures/}{./}} % Paths where images are looked for

% \makeindex[columns=3, title=Alphabetical Index, intoc] % Make LaTeX produce the files required to compile the index

\usepackage{enumitem}
\input{_packages.tex}
\input{_acronyms.tex}
% FIX: Could not find the package that defines the command so there was a ploblem with slides not knowing it before
% redefining <24-01-24> 
\renewcommand{\na}{\(\text{N}/\text{A}\)} 
\input{_commands.tex}


% FIX: There is a vspace or a new paragraph indication when inserting a marginfigure. This can be handled using vspace
% commands manually when the full content is finished <28-12-23> 
% FIX: Reduce the vskip before section and subsection titles <26-12-23> 
% FIX: Do not output chapter numbers in the sections title  <26-12-23> 
% https://en.wikibooks.org/wiki/LaTeX/Counters
% \setcounter{chapter}{-1}
% \setcounter{secnumdepth}{2}

% \includeonly{chapters/01_introduction.tex}
% \includeonly{chapters/02_EM_waves.tex}
% \includeonly{chapters/03_transfer_functions.tex}
% \inlcudeonly{chapters/04_structured_illumination_microscopy.tex}
% \includeonly{chapters/05_harmonic_SIM_reconstruction.tex}
% \includeonly{chapters/06_PSF_estimation.tex}
% \includeonly{chapters/07_conclusion.tex}


\def\documentdate{\displaydate{date}}

\begin{document}
